# Brain3 Repository Coverage Ledger

| Ledger Row | Boundary / Area | Family | Files Checked | Candidate | Disposition | Evidence / Notes |
|---|---|---|---|---|---|---|
| oauth-metadata-host | OAuth metadata and bearer challenge metadata | Host/header trust | `crates/platform/src/http/oauth_handlers.rs`, `crates/platform/src/http/mcp_handlers.rs`, `crates/core/src/application/validate_request.rs` | `brain3-host-header-metadata` | reportable | Metadata builders trust `x-forwarded-host` / `host` directly; host validation is not applied before issuer/resource metadata emission. |
| oauth-redirect-policy | OAuth registrar policy | Redirect URI allowlisting | `crates/platform/src/http/registrar.rs`, `crates/platform/src/http/oauth_handlers.rs` | `brain3-unallowlisted-redirect-uri` | reportable | The configured client ID is fixed, but redirect URIs are caller-selected and only required to be non-empty. |
| oauth-register-surface | OAuth registration surface | Public-client / DCR expansion | `crates/platform/src/http/router.rs`, `crates/platform/src/http/oauth_handlers.rs` |  | not_applicable | No `/oauth/register` route or public-client auth method was found in the current code. |
| oauth-code-lifetime | OAuth authorization code handling | Replay window / binding | `apps/gateway/src/server.rs`, `crates/platform/src/http/state.rs`, local `oxide-auth-0.6.1` source |  | deferred | Auth codes are still minted with a 10-minute lifetime inside `oxide-auth`; the issue remains architectural but was not promoted over stronger gateway-surface findings in this pass. |
| tunnel-default-exposure | Tunnel bootstrap and setup defaults | Remote exposure by default | `crates/core/src/application/first_run_setup.rs`, `crates/platform/src/config/env_file.rs`, `.env.template`, `apps/gateway/src/tui/*` | `brain3-quick-tunnel-default-public-ingress` | reportable | First-run defaults and env fallback still choose Cloudflare quick tunnel unless tunneling is explicitly disabled. |
| tunnel-ingress-config | Named tunnel ingress config writer | Accidental proxy to unrelated localhost ports | `crates/platform/src/tunnel/cloudflare_setup.rs`, `.cloudflared/brain3-macos.yml` |  | suppressed | Current repo config writer and checked-in example both pin ingress to `http://127.0.0.1:<gateway-port>` and end with `http_status:404`. |
| tunnel-credentials-perms | Cloudflare tunnel credential file handling | Local credential exposure | `crates/platform/src/tunnel/cloudflare_setup.rs`, `crates/platform/src/tunnel/cloudflare_named.rs` |  | deferred | The code locates and uses `~/.cloudflared/<id>.json` without a permission check. Still open, but not re-promoted over higher-impact issues. |
| mcp-trace-logging | Gateway and MCP server logging | Sensitive data in logs | `crates/core/src/application/proxy_mcp.rs`, `brain3-mcp-vault-tools/src/brain3_mcp_vault_tools/server.py`, `crates/platform/src/setup/system.rs` | `brain3-sensitive-mcp-trace-logs` | reportable | Trace logging writes MCP bodies; gateway temp log creation does not harden permissions explicitly. |
| vault-path-controls | Vault filesystem tools | Path traversal / vault escape | `brain3-mcp-vault-tools/src/brain3_mcp_vault_tools/vault.py`, `brain3-mcp-vault-tools/src/brain3_mcp_vault_tools/tools/*.py` |  | suppressed | `resolve_vault_path()` rejects null bytes, dot-prefixed components, and resolved paths outside the vault root before reads/writes/moves/deletes. |
| local-credential-storage | `.env`, upstream secret, token DB | Plaintext local secret storage | `crates/platform/src/setup/system.rs`, `crates/platform/src/config/upstream_secret.rs`, `.env.template`, `crates/platform/src/token_store/sqlite.rs` |  | deferred | `.env` and the upstream shared secret remain plaintext on disk, but both are local-only risks with partial mitigations (`0600` on writes). |
